# infraforge module: azure/container-app
# Azure Container App with managed environment and optional ingress.
# Stub — Phase 5+ implementation.

terraform {
  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~> 4.0"
    }
  }
}
